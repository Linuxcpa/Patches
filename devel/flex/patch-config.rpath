$NetBSD$

--- config.rpath.orig	2014-03-24 18:36:42.000000000 +0000
+++ config.rpath
@@ -181,7 +181,7 @@ if test "$with_gnu_ld" = yes; then
       # them.
       ld_shlibs=no
       ;;
-    beos*)
+    haiku*)
       if $LD --help 2>&1 | grep ': supported targets:.* elf' > /dev/null; then
         :
       else
@@ -517,7 +517,7 @@ case "$host_os" in
   amigaos*)
     library_names_spec='$libname.a'
     ;;
-  beos*)
+  haiku*)
     library_names_spec='$libname$shrext'
     ;;
   bsdi[45]*)
