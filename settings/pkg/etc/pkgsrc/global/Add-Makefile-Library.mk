#Adds libraries to build process for all builds

# Adding this when not required does not cause a build failure
BUILDLINK_TRANSFORM+= -lnetwork

#BUILDLINK_TRANSFORM+= -lexpat
#BUILDLINK_TRANSFORM+= -lroot
